pipeline {
    agent {label 'docker-label'}
    stages {
        stage('Build') {
            steps {
            
                git 'https://github.com/vytec-insurance/core-app.git'

                          sh 'docker build -t web:latest .' 
                }
        }
    }
}
